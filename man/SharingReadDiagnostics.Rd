% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{SharingReadDiagnostics}
\alias{SharingReadDiagnostics}
\title{Immutable diagnostics for one Delta Sharing read}
\usage{
SharingReadDiagnostics(
  read_kind,
  response_format,
  table_version = NULL,
  starting_version = NULL,
  ending_version = NULL,
  page_count,
  file_count,
  columns = NULL,
  limit = NULL,
  batch_size,
  concurrency = NULL,
  predicate_hint_sent = FALSE,
  server_limit_hint = NULL,
  min_url_expiration = NULL,
  url_expires_in_seconds = NULL
)
}
\arguments{
\item{read_kind}{\code{"snapshot"} or \code{"cdf"}.}

\item{response_format}{The response format selected for the read.}

\item{table_version}{Resolved snapshot table version, or \code{NULL} for CDF.}

\item{starting_version, ending_version}{Inclusive CDF version bounds, or
\code{NULL} for a snapshot.}

\item{page_count}{Number of protocol response pages consumed during
planning.}

\item{file_count}{Number of file actions selected for the read.}

\item{columns}{Projected columns, or \code{NULL} for all columns.}

\item{limit}{Exact row limit, or \code{NULL} when unbounded.}

\item{batch_size}{Maximum Arrow batch size requested from the reader.}

\item{concurrency}{Selected concurrency, or \code{NULL} for the reader default.}

\item{predicate_hint_sent}{Whether a server-side predicate hint was sent.
Predicate expressions and values are never retained.}

\item{server_limit_hint}{Numeric limit hint sent to the server, or \code{NULL}.}

\item{min_url_expiration}{Earliest signed-file URL expiration selected while
planning, or \code{NULL} when the server supplied no expiration.}

\item{url_expires_in_seconds}{Non-negative time-to-expiry observed when
planning completed, or \code{NULL}. This is an immutable observation, not a
live countdown.}
}
\value{
A read-only \code{SharingReadDiagnostics} object.
}
\description{
\code{SharingReadDiagnostics} is a redacted snapshot of safe planning and
selection facts attached to one stream by \code{\link[=read_arrow_stream]{read_arrow_stream()}}. It never
owns the stream or any execution resource. The object deliberately excludes
credentials, URLs, paths, query strings, page and refresh tokens, predicate
values, protocol actions, and private temporary locations.
}
\details{
Diagnostics are immutable and remain available after stream exhaustion or
explicit release. They do not report an active/released flag because the
standard nanoarrow stream surface does not expose that state reliably.
}
