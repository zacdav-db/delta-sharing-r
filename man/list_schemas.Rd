% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{list_schemas}
\alias{list_schemas}
\title{List schemas}
\usage{
list_schemas(client, share = NULL)
}
\arguments{
\item{client}{A \link{SharingClient}.}

\item{share}{Optional share name. When omitted, schemas in all accessible
shares are listed.}
}
\value{
A base data frame with stable \code{share} and \code{name} character columns.
}
\description{
List schemas
}
\examples{
\dontshow{if (interactive() && nzchar(Sys.getenv("DELTA_SHARING_PROFILE"))) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
client <- sharing_client(Sys.getenv("DELTA_SHARING_PROFILE"))
list_schemas(client, share = "sales")
\dontshow{\}) # examplesIf}
}
