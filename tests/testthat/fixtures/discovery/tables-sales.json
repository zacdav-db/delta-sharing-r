{
  "items": [
    {
      "share": "sales",
      "schema": "default",
      "name": "orders",
      "shareId": "share-sales",
      "id": "table-orders",
      "accessModes": ["url", "dir"],
      "location": "s3://private-bucket/orders?sig=must-not-leak",
      "auxiliaryLocations": [
        "s3://private-bucket/orders-aux?sig=must-not-leak"
      ],
      "credentials": {
        "bearerToken": "table-secret-must-not-leak"
      }
    },
    {
      "share": "sales",
      "schema": "analytics",
      "name": "forecast",
      "id": "table-forecast"
    }
  ]
}
